<%= form_for @group do |f| %>

<div class="form-group">
    <div class="form-group string optional">
        <%= f.label "movie", :class => "string optional control-label" %>
        <%= f.text_field :movie, :class => "string optional form-control" %>
    </div>

    <div class="form-group string optional">
        <%= f.label "review", :class => "string optional control-label" %>
        <%= f.text_area :review, :class => "string optional form-control" %>
    </div>

</div>
<%= f.submit "Submit", class: "btn btn-primary", data: { disable_with: "Submiting..." } %>
<% end %>
