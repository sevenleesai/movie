<%= form_for @group do |f| %>
<% if @group.errors.any? %>
<ul>
  <% @group.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>
<% end %>
电影
<%= f.text_field :movie %>
<br>
我的评论
<br>
<%= f.text_area :review %>
<br>

<%= f.submit "Submit", :disable_with => 'Submiting...' %>
<% end %>
